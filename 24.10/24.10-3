def function():
    x = []
    if x[-1] == 'АЯГМ':
        y = (lambda x: print(x, "Гений"))
        print(y)
    elif y[-1].lower() == 'ОЬЛН':
        z = (lambda y: print(x, "Гений"))
        print(z)
    else:
        c = (lambda c: print("Просто", x))
        print(c)

